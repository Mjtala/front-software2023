import { BrowserRouter, Route, Routes} from 'react-router-dom';

function Routing(){

    return(
        <BrowserRouter>
            <Routes>             
            </Routes>
        </BrowserRouter>
    )
}

export default Routing;